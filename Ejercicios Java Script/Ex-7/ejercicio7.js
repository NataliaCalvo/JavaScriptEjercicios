var text = prompt("Escribe una frase y adivina cuántas letras A contiene");
var nText = text.length;
var a;
for (a = 0; a < nText; a++) {
if (string.substr(a,1) === "a" ) {
document.write(string.substr(a,1));
}
}